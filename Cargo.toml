[package]
name = "animata"
version = "0.2.0"
edition = "2021"
description = "Voxel isometric world on macroquad-3D — environment viewer"

[dependencies]
macroquad = "0.4"

[profile.release]
opt-level = 3
lto = true
