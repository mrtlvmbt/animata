[workspace]
members = ["crates/animata-sim", "crates/animata"]
resolver = "2"

# Profiles live in the workspace root. `lto`/`opt-level=3` for the perf-sensitive sim runs
# (worldgen, erosion, the headless acceptance corridors).
[profile.release]
opt-level = 3
lto = true
