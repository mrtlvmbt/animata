# v2 toolchain pin — independent of v1's root pin (F2). 1.96.0 ≥ bevy_ecs 0.19.0 MSRV (1.95.0),
# and matches v1's 1.96.0 so the determinism story is one toolchain across both workspaces for now.
[toolchain]
channel = "1.96.0"
