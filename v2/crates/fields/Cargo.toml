[package]
name = "fields"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "animata v2 fields backend — conserved resource field, fixed-point integer (M1)"

[dependencies]
sim-core.workspace = true
