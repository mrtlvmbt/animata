[package]
name = "telemetry"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "animata v2 telemetry — Price covariance / means / diversity, read-only sink (M1)"

[dependencies]
sim-core.workspace = true
